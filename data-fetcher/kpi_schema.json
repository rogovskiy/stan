{
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "value": {
      "type": "object",
      "properties": {
        "number": {
          "type": "number"
        },
        "unit": {
          "type": "string"
        },
        "multiplier": {
          "type": ["string", "null"]
        }
      },
      "required": ["number", "unit"]
    },
    "value_type": {
      "type": "string",
      "enum": ["quarterly", "point_in_time", "ttm", "ytd", "change_yoy", "change_mom", "change_qoq"]
    },
    "summary": {
      "type": "string"
    },
    "source": {
      "type": "string"
    },
    "group": {
      "type": "string"
    },
    "frequency": {
      "type": "integer"
    },
    "other_names": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["name", "value", "value_type", "summary", "source", "group", "frequency"]
}
