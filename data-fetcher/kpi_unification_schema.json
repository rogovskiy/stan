{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "previous": {"type": "string", "description": "KPI name from previous quarter"},
      "current": {"type": "string", "description": "KPI name from current quarter"},
      "target": {"type": "string", "description": "Unified/canonical KPI name to use (for normalize and new results)"},
      "target_group": {"type": "string", "description": "Unified/canonical group name to use (for normalize and new results)"},
      "result": {
        "type": "string",
        "enum": ["normalize", "missing", "match", "new"],
        "description": "Type of result: normalize (unify different names), missing (KPI not found), match (exact match), new (new KPI)"
      }
    },
    "required": ["result"]
  }
}
